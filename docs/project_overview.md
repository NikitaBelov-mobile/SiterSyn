# AI Site Generator - Описание и Цели Проекта (Next.js Stack)

## Название проекта
**AI Site Generator** (рабочее название)

## Одностроковое описание
Платформа для мгновенной генерации профессиональных сайтов с помощью AI по одному текстовому запросу с возможностью кастомизации дизайна.

---

## Проблема

### Текущая ситуация на рынке:
- **Профессиональные дизайнеры**: $500-2000 за лендинг, срок 1-2 недели
- **Фрилансеры**: $200-500, срок 3-7 дней, качество непредсказуемо
- **Конструкторы** (Tilda, Wix, Webflow): требуют обучение, времязатратны (4-8 часов)
- **Готовые шаблоны**: не уникальные, требуют адаптации

### Боли целевой аудитории:
1. **Предприниматели**: нужен MVP быстро, бюджет ограничен
2. **Малый бизнес**: нет времени/денег на дизайнера
3. **Фрилансеры**: нужно показать клиенту концепт
4. **Стартапы**: быстрая валидация идеи через посадочную страницу
5. **Агентства**: ускорить процесс создания прототипов

---

## Решение

### Что мы предлагаем:
Сервис, который за **5 минут** превращает текстовое описание в:
- Полностью рабочий сайт
- Профессиональный дизайн
- Адаптивная верстка (mobile/desktop)
- Готовый к публикации
- С возможностью итеративной доработки через чат

### Ключевые преимущества:
- ⚡ **Скорость**: 5 минут вместо дней/недель
- 💰 **Цена**: $15-30 вместо $500+
- 🎨 **Качество**: AI генерирует код уровня senior разработчика
- 🔄 **Гибкость**: неограниченные итерации дизайна
- 📤 **Владение**: экспорт полного кода

---

## Технологический стек (Next.js)

### Почему Next.js + Serverless?

**Критические преимущества:**
1. **Почти нулевые инфраструктурные затраты** ($0-30/мес vs $50-100/мес с Rails)
2. **Автоматическое масштабирование** без настройки
3. **Глобальный CDN** из коробки
4. **Единый язык** (TypeScript) для фронт и бэкенд
5. **Огромная экосистема** React компонентов

### Stack детально

| Layer | Technology | Cost (MVP) |
|-------|-----------|-----------|
| **Framework** | Next.js 14 (App Router) + TypeScript | $0 |
| **Hosting** | Vercel (Hobby tier) | $0 |
| **Database** | Supabase (PostgreSQL + Auth + Storage) | $0 |
| **Cache/Jobs** | Upstash (Redis + QStash) | $0-10 |
| **Storage** | Cloudflare R2 / Supabase Storage | $0-2 |
| **AI** | Claude API (Anthropic) | Pay-per-use |
| **Payments** | Stripe | 2.9% + $0.30 |
| **Email** | Resend | $0 (3K/mo) |
| **Monitoring** | Sentry + Vercel Analytics | $0 |
| **TOTAL** | | **$0-12/мес** |

### Архитектура

```
┌──────────────────────────────────────────────────┐
│         Vercel Edge Network (Global CDN)         │
│  Next.js App + Static Assets + Edge Functions    │
└──────────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────────┐
│         Next.js 14 (App Router)                  │
│  ┌────────────────────────────────────────────┐  │
│  │  Frontend (React + TypeScript)             │  │
│  │  ├── Landing page                          │  │
│  │  ├── Dashboard                             │  │
│  │  ├── Editor (visual + chat)                │  │
│  │  └── Preview                               │  │
│  └────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────┐  │
│  │  API Routes (Serverless Functions)         │  │
│  │  ├── /api/generate                         │  │
│  │  ├── /api/sites/[id]                       │  │
│  │  ├── /api/credits                          │  │
│  │  └── /api/webhooks/stripe                  │  │
│  └────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
            ↓              ↓              ↓
    ┌────────────┐  ┌────────────┐  ┌────────────┐
    │  Supabase  │  │  Upstash   │  │ Claude API │
    │            │  │            │  │            │
    │ PostgreSQL │  │   Redis    │  │ AI Models  │
    │ Auth       │  │   QStash   │  │ + Caching  │
    │ Storage    │  │            │  │            │
    └────────────┘  └────────────┘  └────────────┘
```

### Структура проекта

```
my-site-generator/
├── app/
│   ├── (marketing)/
│   │   └── page.tsx                 # Landing page
│   ├── dashboard/
│   │   └── page.tsx                 # User dashboard
│   ├── editor/[id]/
│   │   └── page.tsx                 # Editor interface
│   ├── api/
│   │   ├── generate/route.ts        # AI generation endpoint
│   │   ├── sites/
│   │   │   ├── route.ts             # CRUD sites
│   │   │   └── [id]/route.ts
│   │   ├── credits/route.ts         # Credit operations
│   │   └── webhooks/
│   │       └── stripe/route.ts
│   └── layout.tsx
│
├── components/
│   ├── GenerationPanel/
│   │   ├── PromptInput.tsx
│   │   ├── StyleSelector.tsx
│   │   └── ReferenceUploader.tsx
│   ├── Editor/
│   │   ├── VisualEditor.tsx
│   │   ├── CodeEditor.tsx
│   │   └── Preview.tsx
│   ├── Chat/
│   │   └── ChatInterface.tsx
│   └── ui/                          # shadcn/ui components
│
├── lib/
│   ├── supabase/
│   │   ├── client.ts                # Browser client
│   │   ├── server.ts                # Server client
│   │   └── types.ts
│   ├── ai/
│   │   ├── claude.ts                # Claude API wrapper
│   │   ├── prompt-builder.ts
│   │   └── toon/                    # TOON encoder/decoder
│   │       ├── encoder.ts
│   │       ├── decoder.ts
│   │       └── dictionary.ts
│   ├── cache/
│   │   ├── redis.ts                 # Upstash Redis
│   │   └── strategies.ts
│   └── stripe/
│       └── client.ts
│
├── public/
│   └── templates/                   # React component templates
│
└── types/
    └── index.ts                     # Shared types
```

---

## Конкурентное преимущество

### Что делает нас уникальными:

1. **TOON оптимизация** (уникальная технология)
   - На 67% дешевле AI costs чем у конкурентов
   - Позволяет предлагать лучшую цену рынка

2. **Serverless = почти нулевые затраты**
   - Vercel FREE tier покрывает MVP полностью
   - Масштабирование автоматическое

3. **Гибридный подход: Templates + AI**
   - Не 100% AI (медленно, дорого)
   - Не 100% templates (негибко)
   - 80% экономия на популярных паттернах

---

## Бизнес-модель

### Pricing Tiers

**FREE tier**:
- 3 генерации при регистрации
- 1 активный сайт
- Поддомен
- Watermark

**STARTER ($15/мес)**:
- 15 генераций/мес
- 3 сайта
- Кастомный домен
- Без watermark

**PRO ($29/мес)**:
- 50 генераций/мес
- 10 сайтов
- AI изображения (20/мес)
- Priority support

**BUSINESS ($79/мес)**:
- Безлимит генераций
- 30 сайтов
- White label
- API access

**Credits (Pay-as-you-go)**:
- 10 = $12, 50 = $49, 100 = $89

---

## Roadmap

### Phase 1: MVP (месяцы 1-3)
- Next.js app + Supabase + Vercel deployment
- Базовая AI генерация с TOON
- 5-7 типов секций
- Stripe integration
- FREE tier launch

**Цель**: 100 users, 10 paying

### Phase 2: Optimization (месяцы 4-6)
- Multi-layer caching
- Template library (10+ шаблонов)
- Prompt caching
- Email automation
- Referral program

**Цель**: $500 MRR

### Phase 3: Scale (месяцы 7-12)
- Background jobs (QStash)
- AI image generation
- Multi-page sites
- Analytics dashboard
- First integrations

**Цель**: $5K MRR, breakeven

---

## Почему сейчас?

- ✅ Next.js 14 App Router стабилен
- ✅ Vercel FREE tier щедрый
- ✅ Supabase mature и reliable
- ✅ Claude API достиг production-quality
- ✅ Serverless устранил infrastructure headache
- ✅ TypeScript everywhere = faster development
- ✅ AI hype создал спрос

---

## Success Criteria (3 месяца)

- ✅ 100+ users
- ✅ 10+ paying customers
- ✅ NPS > 40
- ✅ <$100/мес infrastructure costs
- ✅ Breakeven на AI costs

---

**Дата создания**: 2025-11-17  
**Версия**: 2.0 (Next.js Stack)  
**Статус**: Ready to build
